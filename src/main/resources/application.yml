server:
  port: 8080

spring:
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:mysql://localhost:3306/stockorder
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

