services:
  mysql:
    image: mysql:latest
    container_name: stockorder-mysql
    environment:
      MYSQL_DATABASE: stockorder
      MYSQL_USER: myuser
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"